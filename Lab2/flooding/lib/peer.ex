
# distributed algorithms, n.dulay, 10 jan 22
# basic flooding, v1

defmodule Peer do

  # add your code here, start(), next() and any other functions

  def start do
    IO.puts("Starting Peer")
    # receive do
    #   {:bind, peers} -> next(peers)
    #     # code
    # end
  end

  def next(peers) do
    IO.puts(peers)
  end

end # Peer
